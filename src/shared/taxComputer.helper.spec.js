// Jest Exercice 1
// import { calculerNbParts, computeTaxeService } from "./taxComputer.helper";

describe("TaxComputer tests suite", () => {
  describe("calculerNbParts tests suite", () => {
    test("calculerNbParts doit retourner le bon nombre de part pour deux adultes sans enfant", () => {
      // 🎯 Ajouter ici votre premier test.
      // On veut tester que calculerNbParts(2, O) renvoie 2
    });

    // 🎯 Ajouter d'autres cas
  });

  // 🎯 tester computeTaxeService

  // ❓ devons-nous tester calculImpotParTranche ? Pourquoi ? 💬
});
